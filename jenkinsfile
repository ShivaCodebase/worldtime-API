pipeline {
    agent any
 
    stages {
        stage ('Build') {
            steps {
                sh "mvn -version"
                sh "mvn clean install"
            }
        }

        stage ('final') {
            steps {
                echo 'This is a minimal pipeline.'
            }
        }
    }
}
